'use strict';

var dataShaper = require('./');

function fetchData(id, reference, collection) {
    console.log(id, reference);
    process.exit();
}

dataShaper({
    id: 2,
    name: 'Kristoffer',
    companyId: 3
}, {
    collectionName: 'persons',
    shape: {
        id: 'id',
        name: 'name',
        company: 'companyId.name'
    }
}, {
    fetchData: fetchData
}, function(err, res) {
    console.log(err, res);
});
